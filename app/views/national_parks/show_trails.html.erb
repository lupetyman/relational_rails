<a href="/national_parks">National Park Index</a>

<h1>Trails at <%= @national_park.name %> National Park</h1>

<% @trails.each do |trail| %>
  <h3><a href="/trails/<%= trail.id %>"><%= trail.name %></a></h3>
  <p>Length: <%= trail.length %> miles</p>
  <p>Loop?: <%= trail.is_loop %></p>
  <p>Created At: <%= trail.created_at %></p>
  <p>Updated At: <%= trail.updated_at %></p>
  <br>

  <!-- <table>
    <tr>
      <td>National Park</td>
      <td><%= NationalPark.find(trail.national_park_id).name %></td>
    </tr>
    <tr>
      <td>Length</td>
      <td><%= trail.length %> miles</td>
    </tr>
    <tr>
      <td>Loop?</td>
      <td><%= trail.is_loop %></td>
    </tr>
    <tr>
      <td>Created At</td>
      <td><%= trail.created_at %></td>
    </tr>
    <tr>
      <td>Updated At</td>
      <td><%= trail.updated_at %></td>
    </tr>
  </table>
  <br> -->
<% end %>
